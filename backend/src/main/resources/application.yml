server:
  port: 8080

spring:
  application:
    name: la-vurger-api

  datasource:
    url: jdbc:postgresql://localhost:5432/la_vurger_db
    username: postgres
    password: lavurger_password_123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
