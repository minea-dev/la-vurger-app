version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: lavurger-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: lavurger_password_123
      POSTGRES_DB: la_vurger_db
    restart: always
